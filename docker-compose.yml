services:
  test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: compiler-test
    volumes:
      - .:/workspace
      - ~/.m2:/root/.m2
    working_dir: /workspace
    command: mvn test